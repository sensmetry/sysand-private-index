# Submitted packages

This folder holds the team's submitted `.kpar` files, exactly as
`sysand build` produced them; see **"Publish a project"** in the
[repository README](../README.md) for the step-by-step guide. Files stay
here as the record of what has been submitted. Published versions are
immutable, so never change a file here; publish a new version instead.
